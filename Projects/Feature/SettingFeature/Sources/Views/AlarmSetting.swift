import SwiftUI
import Designsystem

struct AlarmSetting: View {
    var body: some View {
        ZStack {
            JColor.background.ignoresSafeArea()
        }
        .navigationTitle("알림")
    }
}
