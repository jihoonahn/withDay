import SwiftUI
import Rex
import Designsystem

struct LanguageView: View {
    var body: some View {
        ZStack {
            JColor.background.ignoresSafeArea()
            ScrollView {
                VStack(spacing: 16) {
                    
                }
            }
        }
        .navigationTitle("언어 설정")
    }
}
